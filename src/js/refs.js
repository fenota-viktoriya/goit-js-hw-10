export { formRef, listRef, boxRef };

const formRef = document.querySelector('#search-box');
const listRef = document.querySelector('.country-list');
const boxRef = document.querySelector('.country-info');
